# T3 · 真题长拆解档

> **最长的一档。** 一道真题（通常四问）从头拆到尾，允许 4–6 分钟。
> 真源：`特别期_A题求解思路`（286.37s）、`特别期_B题开打指南`（319.10s）

---

## 1. 适用 / 不适用

| ✅ 适用 | ❌ 不适用 |
|---|---|
| 一道真题四问完整走一遍 | 短清单（→ T2） |
| 真题求解思路复盘 | 论文图逐张讲解（→ T4） |
| 赛期内「这题怎么打」指南 | 赛事资讯（→ T5） |
| 时长 4–6 分钟 | 时长 < 2 分钟 |

**判断**：主体是「一道真题的完整求解过程」→ 用本档。

---

## 2. 规格

| 项 | 值 |
|---|---|
| 时长 | 240–320s（实测 286.37s / 319.10s） |
| 场景数 | 6–10（实测 9 / 10） |
| 口播字数 | 1500–2000 字 |
| rate | `+20%`（**密读降档**，不用 +28%——长片再快就糊了） |
| 音色 | `zh-CN-YunxiNeural` |
| GAP | 0 |
| fps | 30 |
| 帧数 | 8591（A 题）/ 9573（B 题） |
| 输出 | **只出横版**（用户 2026-09-10 拍板不上竖版）+ 双封面 |
| 字幕 | 词边界实时刻，硬切无动画 |

---

## 3. 与其他档的关键差异

| 维度 | T1 | **T3** |
|---|---|---|
| 时长 | 185–195s | **286–319s** |
| rate | +28% | **+20%**（降档） |
| 场景数 | 9–11 | 6–10（每场景更长） |
| 竖版 | 出 | **不出** |
| 数值口径 | 单一 | **必须分「题面事实」和「本队实算」** |
| 观点密度 | 判据 / 口诀为主 | **必须有自拟观点**（用户明确要求「要有自己的想法，不模板化」） |

### 版式序（沿用 T1 的版式，只是排列不同）
```
A 题：hook → route → formula → compare → checklist → number(danger) → checklist → formula → text
B 题：hook → route → compare → compare → checklist → checklist → formula → formula → compare → text
```
⚠️ 长片别连续堆 3 个同类版式，中间要插 `number(danger)` 或 `checklist` 打断，否则观众会掉线。

---

## 4. 数值口径规则（本档特有，最容易出事）

真题拆解会同时出现两类数字，**必须分开处理**：

| 类别 | 例子 | 口播写法 |
|---|---|---|
| **题面事实**（写死） | 25cm / 2cm、28°C、2.55→0.15、50°C 平台、附录公式系数、附件2 收缩 1.2cm | 直接说死 |
| **本队实算结果**（真实解出来但只对本队成立） | 56.5h / 123h / 36.7°C / 6.4% / ±20%→∓14% / +22% | **必须带**「大约」「实算出来」，不承诺普适 |

**反例**：「第三问最优时间是 56.5 小时」→ 听感像官方答案。
**正例**：「我们实算出来，第三问大约五十六个半小时」。

---

## 5. 内容要求：不模板化

用户明确要求过「要有自己的想法，不模板化」。每期至少要有 **2–3 个自拟观点**，不是复述题目。

参考 A 期的三个自拟观点：
1. **归因先参数后机制**（Q4 陷阱：123h 不等于全是收缩的功劳，附录三 vs 四的 D 指前差五倍多）
2. **表面温度 ≠ 烘房温度**（Robin 边界坑：设 50°C 恒温边界是错的，表面是解出来的，30min 才 36.7°C）
3. **题面空白用数据钉死**（口径决策：烘房外推取平台值 / 盯中心点 / t_pre 斜率判据 / 端面 7% 量化假设）

**做法**：在 S4 蓝图里单列一节「本期自拟观点」，让用户确认哪些保留——这是这档的价值所在。

---

## 6. 叙事结构建议

不要平铺四问，找一个**统摄概念**把四问串起来。

| 期 | 统摄概念 | 效果 |
|---|---|---|
| B 期 | 四问逐个拆（成本模型 → Q1 → Q2 → Q3 → Q4） | 清晰但平 |
| **A 期（更优）** | **「两只时钟」**：一道题里 Q1 是 30 分钟、Q3 是 56.5 小时，差 17 倍 | 四问压成「一台求解器四个开关」，记忆点强 |

**结尾用金句收束**，别用 checklist 结束。A 期：「A题奖励的是把物理常识翻译进方程的人」。

---

## 7. 执行清单

```powershell
$env:PYTHONPATH="C:\Users\92182\AppData\Roaming\Python\Python312\site-packages"
& "C:\Program Files\Python312\python.exe" _gen_tts.py       # 注意 RATE = "+20%"
# 校验：_check_sync.py（podcast.txt ↔ VOICE_TEXTS ALL MATCH）
python _verify_timing.py                                    # 词边界 9/9 段对齐
npx remotion studio src\remotion\index.ts                   # 逐场景目检
npx remotion render src\remotion\index.ts <LandscapeId> out\横版.mp4 --gl=angle
# 后处理两关（loudnorm + yuv420p/bt709）
```

**抽帧 QA 点**（长片必做，中段最容易 overflow）：
- 10% 片头：`--frame=763`
- 密集中段：`--frame=3329` / `6621` / `7729` → 渲染 4 帧 probe，确认无 overflow 警告
- 片尾：`--frame=8450`

---

## 8. 已交付参考

| 期 | 时长 | 帧数 | 场景 | rate | 备注 |
|---|---|---|---|---|---|
| 特别期A · A题求解思路 | 286.37s（4:46） | 8591 | 9 | +20% | 只出横版；修正版 v2 改了三处数字口径后重渲 |
| 特别期B · B题开打指南 | 319.10s（5:19） | 9573 | 10 | +20% | 只出横版；首次验证 `风格规范\template\` |

⚠️ **两期都改过数字口径后重渲**——说明长片的数字核对必须在 S3 做透，不要在渲染后发现。
