# T4 · 奶油论文图解档

> **唯一一档浅色风格。** 奶油底 + 左文右图，逐句讲论文截图 / 题目原图，轻量亲和。
> 真源：`_tools_pipeline\EpisodeScene.tsx` + `cumcm2021b-video`（2021 国赛乙醇制备 C4 烯烃）

---

## 1. 适用 / 不适用

| ✅ 适用 | ❌ 不适用 |
|---|---|
| 论文里的图 / 表逐张讲 | 深空风系列正片（→ T1） |
| 题目原图展示 + 讲解 | 紧迫清单（→ T2） |
| 想要「不那么严肃」的亲和感 | 一道真题完整求解（→ T3） |
| 时长 3–4 分钟 | 赛事资讯（→ T5） |

**判断**：主体是「论文里的图 / 题目原图」逐张讲 → 用本档。

---

## 2. 规格

| 项 | 值 |
|---|---|
| 时长 | 220–260s（实测 242.15s = 4:02） |
| 场景 | **6 章节**（open / q1 / q2 / q3 / q4 / end），**30 句** cue |
| 口播字数 | 1300–1500 字 |
| rate | `+8%`（**慢速讲解**，比系列的 +20% 慢很多） |
| 音色 | `zh-CN-YunjianNeural`（**云健**，与其他档的云希不同） |
| GAP | **0.18s**（逐句间隙，增强自然感） |
| fps | 30 |
| 帧数 | 7264 |
| 输出 | 横版 + 双封面（`封面-横屏4-3.png` / `封面-竖屏3-4.png`） |
| 字幕 | 逐句条（每句一条字幕，句间 0.18s 间隙） |

---

## 3. 风格 DNA

### 3.1 色板（浅色，与其他 4 档完全不同）
```ts
const CREAM    = "#FDFBF4";   // 主底（奶油）
const BLUE_DEEP = "#1E3A8A";  // 要点正文 / 深蓝
const YELLOW   = "#F4D758";   // 要点卡描边
const TEAL     = "#0E9F9E";   // 默认章节色
const INK      = "#1A1A2E";   // 主文字
const INK_FAINT = "#7A7A8C";  // 次要文字
const SANS = "'PingFang SC', 'Microsoft YaHei', sans-serif";
```

### 3.2 章节颜色（6 章节各一色）
```ts
const CH_HUE = {
  open: "#0E9F9E",   // 青
  q1:   "#3B82F6",   // 蓝
  q2:   "#F59E0B",   // 橙
  q3:   "#8B5CF6",   // 紫
  q4:   "#EF4444",   // 红
  end:  "#1E3A8A",   // 深蓝
};
const CH_LABEL = {
  open: "国赛真题剖析",
  q1: "第一问 · 温度相关",
  q2: "第二问 · 多变量影响",
  q3: "第三问 · 全局寻优",
  q4: "第四问 · 实验设计",
  end: "收束",
};
```

### 3.3 布局（**单版式**：左文右图）
```
┌────────────────────────────────────────────┐
│▓▓▓▓▓▓▓▓▓▓▓▓▓ 顶 14px 三色渐变条            │
│  ┌────────┐                    数模AI冲国奖 │  ← 章节胶囊（accent 底白字）+ 系列名
│  │        │                                │
│  │▍章节名 │   ┌──────────────────────────┐ │
│  │ (宋体) │   │                          │ │
│  │ 副标签 │   │      论文图 / 题目原图    │ │  ← 右侧 34% 宽
│  │        │   │      （白卡片 + 阴影）    │ │
│  │┌─────┐ │   │                          │ │
│  ││本句 │ │   └──────────────────────────┘ │
│  ││要点 │ │                                │
│  │└─────┘ │                                │  ← 左侧要点卡：白底 + 2px 黄描边
│  └────────┘                                │
│        ┌──────────────────────────────┐    │
│        │ 本句口播字幕（深色半透明圆角）│    │
│        └──────────────────────────────┘    │
│▓▓▓▓▓▓▓▓▓▓▓▓▓▓ 底 14px 三色渐变条            │
└────────────────────────────────────────────┘
```

**开场段（open）特殊**：左右分栏改成**整屏居中**显示原题图，亮出题目。

### 3.4 背景两层（比深空档简单得多）
```
① backgroundColor: CREAM (#FDFBF4)
② 44px 网格：双 linear-gradient 1px 线，rgba(26,26,46,0.035)
③ 顶底各 14px：linear-gradient(90deg, #3B82F6 55%, #F4D758 78%, #E84A5F 100%)
```

### 3.5 字号
| 元素 | 横版 |
|---|---|
| 章节胶囊 | 30px (800) |
| 系列名 | 26px (700) |
| 小节标题（宋体） | 46px (900) |
| 副标签 | 26px (600) |
| 要点卡标题 | 30px (800) |
| 要点卡正文 | 36px (700) |
| 底部字幕 | 40px (600) |

---

## 4. 章节与字幕机制

### 4.1 章节边界硬编码（本档特有）
章节切分靠**句子索引**硬编码，不是从数据推导：
```python
SECTION_BOUNDARIES = {"open":0, "q1":9, "q2":14, "q3":20, "q4":25, "end":28}
SECTION_NAMES = ["open","q1","q2","q3","q4","end"]
```
⚠️ 改口播稿的句子数后，**必须同步改这两个常量**，否则章节标签错位。这是本档最容易踩的坑。

### 4.2 逐句字幕（本档核心）
每一行口播 = 一句 TTS = 一条字幕，逐句计时：
```python
gap = 0.18   # 句间静音间隙（秒），增强自然感
for i, (wav, ln) in enumerate(zip(wavs, lines)):
    d = dur(wav)
    cues.append({"idx":i, "text":ln, "start":round(t,3), "end":round(t+d,3), "duration":round(d,3)})
    t = d + gap
```
字幕条：句内进度 `localT` 驱动，淡入 + 上移 26px，`localT` 0→0.06 淡入、0.94→1 淡出。

### 4.3 图片动画（KenBurns，只在切图时触发）
```tsx
const imgKey = p.img;      // 图片作为动画触发键：仅当图片变化时重挂载
// 同图内多句不重放 —— 动画进度基于「图片首次出现后的累计时间」
const imgP   = clamp((t - CUES[firstIdx].start) / 3.5, 0, 1);
const imgZoom = interpolate(imgP, [0,1], [isOpen ? 1.02 : 1.05, isOpen ? 1.08 : 1.12], ...);
const imgFade = interpolate(imgP, [0,0.35], [0,1], ...);
const imgX    = interpolate(imgP, [0,1], ["3%","0%"], ...);
```

---

## 5. 数据层（displayPlan）

每句一条 `{ img, chapter, note }`：
- `img`：这一句展示哪张图（同图多句只播一次动画）
- `chapter`：左侧小节标题（宋体大字）
- `note`：本句要点卡正文

图素材放在工程 `public\`，组件里 `staticFile(p.img)` 引用。真源中的图命名参考：
`open_p1.png` / `open_p2.png`（原题）、`card_q1.png` / `lay_q1b.png` / `lay_q1c.png`（论文卡片图）、`verify_px.png` / `verify_constraint.png`（验算图）。

---

## 6. 执行清单

本档的脚本链路与深空档**不同**（是逐句手工拼接，不是 `_gen_tts.py`）：

```powershell
$env:PYTHONPATH="C:\Users\92182\AppData\Roaming\Python\Python312\site-packages"
# ① 逐句 TTS：每行一句 → 各自合成 → 测时长 → 拼接 → 精确 SRT + timing.json
& "C:\Program Files\Python312\python.exe" gen_tts_sentence.py
# ② 若已有 part wav（_tts_parts\part_NN.wav），只重拼音轨
& "C:\Program Files\Python312\python.exe" _stitch_wav.py
# ③ 同步校验
& "C:\Program Files\Python312\python.exe" check_sync.py
& "C:\Program Files\Python312\python.exe" _final_check.py
# ④ 渲染
npx remotion render src\remotion\index.ts <Id> <ep>_无BGM.mp4 --gl=angle
# ⑤ 手工混 BGM（本档没有 --mix 参数，用 ffmpeg amix，BGM 0.10）
```

**关键参数**（`gen_tts_sentence.py` 顶部改）：
```python
VOICE  = "zh-CN-YunjianNeural"
RATE   = "+8%"
FPS    = 30
gap    = 0.18      # 句间静音
SECTION_BOUNDARIES = {...}   # ★ 改句子数必改
```

---

## 7. 与其他档的差异速查

| 维度 | T1（深空） | **T4（奶油）** |
|---|---|---|
| 底色 | `#070B18` 深空 | **`#FDFBF4` 奶油** |
| 字幕底色 | `rgba(7,11,24,0.80)` | `rgba(26,26,46,0.82)`（同思路，浅底深条） |
| TTS 粒度 | 每场景一段 | **每句一段**（30 句） |
| 音色 | 云希 `YunxiNeural` | **云健 `YunjianNeural`** |
| rate | +20% / +28% | **+8%** |
| 章节切分 | 从 `[SECTION:name]` 推导 | **句子索引硬编码** |
| 图片 | 少数装饰 | **主体内容** |

---

## 8. 已交付参考

`_tools_pipeline\cumcm2021b_带BGM.mp4` / `cumcm2021b_无BGM.mp4`
- 242.15s（4:02），7264 帧 @30fps，30 句，6 章节
- 发布标题：「国赛B题拆解：C4烯烃收率怎么顶」
- 章节时间戳：0:00 开场 / 0:09 四问总览 / 1:06 第一问 / 1:49 第二问 / 2:30 第三问 / 3:16 第四问 / 3:42 总结

⚠️ 本档的 `SECTION_BOUNDARIES` 是硬编码的，**复用时改动成本最高**——如果口播稿结构会频繁变，优先用 T1 / T3。
